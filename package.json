{
  "name": "vezmenov.dev",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "ui:snap": "npm run build && node scripts/ui-snapshots/snap.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.58.2",
    "vite": "^6.4.1"
  },
  "dependencies": {
    "three": "^0.182.0"
  },
  "engines": {
    "node": ">=18"
  }
}
